<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm my-2 inline-block" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5"><span class="font-light">Hello</span> <%= user.name %></h3>
        <h5 class="mb-5">You can create a new post</h5>
        <form action="/posts" method="post">
            <textarea name="content" placeholder="What's on your mind..." class=" p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input type="submit" class="px-3 py-2 bg-blue-500 block rounded-md mt-2 W-40 text-sm cursor-pointer"  value="Create new Post"/>
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts</h3>
            <div class="postcontainer mt-5 gap-4 flex">
                <% user.posts.reverse().forEach(function(post){ %> 
                    <div class="post w-1/3 p-5 border-2 mb-4 rounded-md border-zinc-800">
                        <h5 class="text-blue-500 mb-2">@<%= user.username %></h5>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <div class="btns flex mt-5 gap-4">
                            <a class="text-blue-500">Like</a>
                            <a class="text-zinc-600">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>